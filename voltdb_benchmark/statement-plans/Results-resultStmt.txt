SQL: SELECT book_name, book_id,  COUNT(*) as num_reviews     FROM reviews, books where reviews.book_id = books.book_id GROUP BY book_name,book_id order by num_reviews desc;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    NESTLOOP INDEX INNER JOIN
     inline Hash AGGREGATION ops: COUNT(
     inline INDEX SCAN of "BOOKS" using its primary key index
      uniquely match (BOOK_ID = REVIEWS.BOOK_ID)
     SEQUENTIAL SCAN of "REVIEWS"

